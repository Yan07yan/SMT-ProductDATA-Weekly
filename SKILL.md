---
name: ae-weekly-action-report
description: 速卖通 POP+半托管 多店《周度行动表》生成器（导出驱动）。当用户说"跑周报"、要生成本周运营行动清单、或要用平台导出文件核算公司周/月度 KPI（销售额/刊登数/热销链接/全托管订单/滞销清理/利润率）时使用。脚本自动解析四店30天数据、订单销售额、全托结算、商品总数据、列表导出与上品台账，输出 4 页 Excel（KPI缺口/行动清单/数据底稿/上周好品类与本周方向）。支持双轨：30天文件保KPI稳定，另加「上周窗口」产品文件专供「上周卖得好的品类」与「本周上品方向」草稿。
version: 1.0
type: workflow
agent_created: true
---

# 速卖通《周度行动表》生成器（导出驱动）

> 唯一执行脚本：`scripts/build_weekly_action.py`（与本 SKILL.md 同目录）。
> 导出清单参考：`references/每周导出勾选检查表.md`（每周一对着打勾）。
> 上品台账模板：`assets/刊登数量追踪_模板.xlsx`（首次运行脚本也会自动建）。

## 一、何时使用
- 用户每周一导出平台数据后说"跑周报""出周报""生成周度行动表"。
- 用户要核算公司月度 KPI 的本周进度：销售额、刊登数量、热销链接、全托管订单、滞销清理、利润率。
- 用户要一份可直接照做的本周行动清单（先补哪个 KPI 缺口、哪些品改主图/详情/冲热销）。

## 二、关键口径（必须先讲给用户听，否则算出来是错数）
1. **销售额 = 四店 POP 订单月化 + 全托 30 天原值**
   - POP 销售额来自「五店销售额订单数据」.xls 的 `产品总金额`（=GMV），按订单窗口 `(最晚−最早+1)` 天做 `×(30/窗口天数)` 月化。⚠️ 窗口仅几天时月化为估算，建议改导「月至今/自然月」口径直接对标。
   - 全托销售额来自结算文件 `实际结算金额`（CLYT1 的 30 天数据**无金额列**，不能从这里取）。
2. **利润率 = 订单供货价估算的商品级毛利（扣 3% 平台佣金）**
   - 用 `发货产品供货价×数量` 作成本，净率 = (GMV−成本)/GMV − 3%；未扣头程运费/退款/平台其他费，是**上限**。分店净率单独列出。
3. **刊登数量 = 用户每日维护的「刊登数量追踪.xlsx」**
   - CLYT1 列 = 全托管；YZ3/WHDS06/PHCX04/ZHHK04 = POP 四店。脚本按 `--week-start/--week-end` 窗口汇总，默认上一完整自然周（周一~周日）。
   - 该文件缺失时脚本自动建带表头的模板并提示填报后重跑（当周刊登数=0）。
4. **热销链接 = 四店 + 全托近 30 天支付件数 ≥ 15 的品数。**
5. **滞销品 = 全量商品目录中近 30 天 0 支付件数的品**（依赖「商品总数据」必须 Excel 打开→保存一次落盘，否则空壳、本项为"暂停"）。
6. **上品通过率 = 列表导出 ID 差集法**（本周在线 ID − 上周快照基线）；首次运行是"基线周"，真实率下周出。
7. **双轨 + 上周好品类（页4）**
   - 30 天产品文件(`*-30天数据.xlsx`)继续算 KPI（热销/滞销/流量保稳定，避免 7 天窗口误判零动销）。
   - 另需导出「上周窗口」产品文件 `*-上周数据.xlsx`，脚本按 `根类目名称` 聚合出**上周卖得好的品类**（件数/金额/占店比），并在页4 给**本周上品方向**草稿 + 留★手填列。
   - 缺 `*-上周数据.xlsx` 时自动回退 `*-30天数据.xlsx` 并在页4 标⚠️（数字为 30 天口径非真实上周）。

## 三、每周一需导出的文件（放进同一目录 `--data`）
| 文件 | 作用 | 提醒 |
|---|---|---|
| `YZ3-30天数据.xlsx` 等 4 个 | 四店流量/转化/成交（算 KPI） | sheet 名须含「汇总指标」 |
| `YZ3-上周数据.xlsx` 等 4 个 | 上周好品类（页4） | 上周窗口导出；缺则回退30天文件标⚠️ |
| `YZ3商品总数据.xlsx` 等 4 个 | 全量目录（算滞销） | ⚠️ **必须 Excel 打开→保存一次**再丢回，否则空壳 |
| `五店「销售额订单数据」.xls` | POP 销售额 + 利润率 | 含 `产品总金额`/`发货产品供货价` |
| `QTG-GLYT1 近三十天结算.xlsx` | 全托销售额 | 取 `实际结算金额` |
| `四店「商品管理」列表导出.xls` | 通过率（ID差集） | 含 `ItemId`/`来源渠道` |
| `CLYT1全托30天数据.xlsx` | 全托热销/订单 | 取 `支付件数` |
| `全托管上架数据.xlsx` | （预留）全托上架 | 当前以追踪表为主，此文件可空 |
| `刊登数量追踪.xlsx` | 刊登数（每日维护） | 缺失自动建模板 |

完整勾选清单见 `references/每周导出勾选检查表.md`。

## 四、运行方式
```bash
# 用 WorkBuddy 托管 Python（已含 openpyxl/xlrd）
"<python路径>/python.exe" "<本skill目录>/scripts/build_weekly_action.py" \
  --data "<每周导出目录>"

# 指定报告日期与上周窗口（如用户给出特定上架窗口）
".../build_weekly_action.py" --data "<dir>" \
  --date 2026-09-14 --week-start 2026-09-08 --week-end 2026-09-12

# 自定义输出/快照/追踪表位置
".../build_weekly_action.py" --data "<dir>" \
  --out "<输出.xlsx>" --snapshot "<快照.json>" --track "<追踪表.xlsx>"
```
- 默认输出：`<data>/速卖通_周度行动表_<date>.xlsx`
- 默认快照：`<data>/_listing快照.json`（**跨周持久化，勿删**，否则通过率永远停在基线周）
- 默认追踪表：`<data>/刊登数量追踪.xlsx`（缺失自动建模板）
- 依赖（仅首次）：`python -m pip install openpyxl xlrd`
- **账号专属，同事接入时必改**：脚本顶部 `SALES_SHOP` / `SHOP_FILE` / `CAT_FILE` 的店铺代号（YZ3/WHDS06/PHCX04/ZHHK04/CLYT1）与 `KPI` 月度目标，按自己账号与当月考核改。

## 五、参数说明
| 参数 | 默认 | 说明 |
|---|---|---|
| `--data` | `./每周导出` | 每周导出数据目录 |
| `--date` | 今天 | 报告日期 `YYYY-MM-DD` |
| `--week-start` / `--week-end` | 上一完整周(一~日) | 刊登追踪汇总窗口 |
| `--out` | `<data>/速卖通_周度行动表_<date>.xlsx` | 输出路径 |
| `--snapshot` | `<data>/_listing快照.json` | ID 快照（持久化） |
| `--track` | `<data>/刊登数量追踪.xlsx` | 上品台账（缺失建模板） |

## 六、输出（3 个 sheet）
| Sheet | 内容 |
|---|---|
| 1_公司KPI与本周缺口 | 6 大 KPI 的月度目标/当前/缺口/本周应补/权重/判断动作；头条先列缺口最大的项 |
| 2_本周行动清单 | 本周重点头条 + 热销冲刺 TOP10 + 主图优化 TOP10 + 详情页优化 TOP10 + 上品通过率复盘 |
| 3_数据底稿 | 热销候选、滞销 TOP20、四店流量 TOP15（支撑页1-2） |
| 4_上周好品类与本周方向 | 各店上周好品类 TOP6（件数/金额/占店比）+ 本周上品方向草稿与★手填列 |

## 七、执行步骤（Agent 收到"跑周报"时）
1. 确认 `--data` 目录已放齐第三节的文件；若缺「商品总数据」未打开保存，提醒用户先处理（否则滞销项暂停）。
2. 运行脚本生成 xlsx（必要时带 `--date` / `--week-start` / `--week-end`）。
3. 读回关键数字（销售额月化、利润率净率、刊登数 POP/全托拆分、热销、全托订单、滞销、通过率是否基线周）回填给用户。
4. 若某 KPI 明显不达标，按页2 头条优先级给动作（先补缺口最大的 KPI，再做产品优化）。
5. 提醒：通过率本周若为"基线周"，下周重跑才出真实值；追踪表需用户每天填。

## 八、硬性约束
- 不在画布创建自动化实例；本 Skill 只提供可复用脚本，按需手动/定时运行。
- 「商品总数据」空壳时，严禁把滞销数报成 0（必须显示"暂停/请打开保存"），避免误判。
- 销售额月化是估算，给用户看时必须标注"窗口仅 N 天，建议改导月至今"。
- 店铺代号与 KPI 目标是账号/月度专属，换同事/换月必须改 `config.json`，不要当成通用值（脚本代码不含真实店铺 ID）。
